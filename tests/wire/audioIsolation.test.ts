// This file was auto-generated by Fern from our API Definition.

import { ElevenLabsClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("AudioIsolationClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            items: [
                {
                    id: "id",
                    title: "title",
                    created_at_unix: 1,
                    format: "format",
                    duration_seconds: 1.1,
                    download_url: "download_url",
                    icon_url: "icon_url",
                    source_video_url: "source_video_url",
                    supports_video: true,
                    processing: true,
                    video_processing_failed: true,
                    preview_b64: "preview_b64",
                },
            ],
            has_more: true,
        };
        server
            .mockEndpoint()
            .get("/v1/audio-isolation/history")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.audioIsolation.list({
            pageSize: 1,
            page: 1,
            search: "search",
        });
        expect(response).toEqual({
            items: [
                {
                    id: "id",
                    title: "title",
                    createdAtUnix: 1,
                    format: "format",
                    durationSeconds: 1.1,
                    downloadUrl: "download_url",
                    iconUrl: "icon_url",
                    sourceVideoUrl: "source_video_url",
                    supportsVideo: true,
                    processing: true,
                    videoProcessingFailed: true,
                    previewB64: "preview_b64",
                },
            ],
            hasMore: true,
        });
    });

    test("delete", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .delete("/v1/audio-isolation/history/history_item_id")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.audioIsolation.delete("history_item_id");
        expect(response).toEqual(undefined);
    });
});
