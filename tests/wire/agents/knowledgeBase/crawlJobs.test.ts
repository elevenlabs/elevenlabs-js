// This file was auto-generated by Fern from our API Definition.

import { ElevenLabsClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";

describe("CrawlJobsClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            crawl_jobs: [
                {
                    type: "discovery",
                    seed_url: "seed_url",
                    pattern: "pattern",
                    max_depth: 1,
                    max_pages: 1,
                    status: "queued",
                    pages_identified: 1,
                    pages_scraped: 1,
                    pages_skipped: 1,
                    pages_failed: 1,
                    root_folder_id: "root_folder_id",
                    updated_at: 1,
                    id: "id",
                    created_at: 1,
                },
            ],
            next_cursor: "next_cursor",
        };

        server
            .mockEndpoint()
            .get("/v1/convai/knowledge-base/crawl")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.agents.knowledgeBase.crawlJobs.list({
            includeJobIds: ["include_job_ids"],
            pageSize: 1,
            cursor: "cursor",
        });
        expect(response).toEqual({
            crawlJobs: [
                {
                    type: "discovery",
                    seedUrl: "seed_url",
                    pattern: "pattern",
                    maxDepth: 1,
                    maxPages: 1,
                    status: "queued",
                    pagesIdentified: 1,
                    pagesScraped: 1,
                    pagesSkipped: 1,
                    pagesFailed: 1,
                    rootFolderId: "root_folder_id",
                    updatedAt: 1,
                    id: "id",
                    createdAt: 1,
                },
            ],
            nextCursor: "next_cursor",
        });
    });

    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { url: "url" };
        const rawResponseBody = {
            id: "id",
            type: "discovery",
            root_folder_id: "root_folder_id",
            status: "status",
            created_at: 1,
            folder_path: [{ id: "id" }],
        };

        server
            .mockEndpoint()
            .post("/v1/convai/knowledge-base/crawl")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.agents.knowledgeBase.crawlJobs.create({
            url: "url",
        });
        expect(response).toEqual({
            id: "id",
            type: "discovery",
            rootFolderId: "root_folder_id",
            status: "status",
            createdAt: 1,
            folderPath: [
                {
                    id: "id",
                },
            ],
        });
    });

    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            type: "discovery",
            seed_url: "seed_url",
            pattern: "pattern",
            max_depth: 1,
            max_pages: 1,
            status: "queued",
            pages_identified: 1,
            pages_scraped: 1,
            pages_skipped: 1,
            pages_failed: 1,
            root_folder_id: "root_folder_id",
            updated_at: 1,
            id: "id",
            created_at: 1,
        };

        server
            .mockEndpoint()
            .get("/v1/convai/knowledge-base/crawl/crawl_job_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.agents.knowledgeBase.crawlJobs.get("crawl_job_id");
        expect(response).toEqual({
            type: "discovery",
            seedUrl: "seed_url",
            pattern: "pattern",
            maxDepth: 1,
            maxPages: 1,
            status: "queued",
            pagesIdentified: 1,
            pagesScraped: 1,
            pagesSkipped: 1,
            pagesFailed: 1,
            rootFolderId: "root_folder_id",
            updatedAt: 1,
            id: "id",
            createdAt: 1,
        });
    });

    test("cancel", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/convai/knowledge-base/crawl/crawl_job_id/cancel")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.agents.knowledgeBase.crawlJobs.cancel("crawl_job_id");
        expect(response).toEqual({
            key: "value",
        });
    });
});
