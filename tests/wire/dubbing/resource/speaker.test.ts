/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../../../mock-server/MockServerPool";
import { ElevenLabsClient } from "../../../../src/Client";
import * as ElevenLabs from "../../../../src/api/index";

describe("Speaker", () => {
    test("update (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = { version: 1 };
        server
            .mockEndpoint()
            .patch("/v1/dubbing/resource/dubbing_id/speaker/speaker_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.dubbing.resource.speaker.update("dubbing_id", "speaker_id");
        expect(response).toEqual({
            version: 1,
        });
    });

    test("update (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { voice_id: undefined, languages: undefined };
        const rawResponseBody = { detail: undefined };
        server
            .mockEndpoint()
            .patch("/v1/dubbing/resource/dubbing_id/speaker/speaker_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.dubbing.resource.speaker.update("dubbing_id", "speaker_id", {
                voiceId: undefined,
                languages: undefined,
            });
        }).rejects.toThrow(ElevenLabs.UnprocessableEntityError);
    });

    test("find_similar_voices (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            voices: [
                {
                    voice_id: "voice_id",
                    name: "name",
                    category: "premade",
                    description: "description",
                    preview_url: "preview_url",
                },
            ],
        };
        server
            .mockEndpoint()
            .get("/v1/dubbing/resource/dubbing_id/speaker/speaker_id/similar-voices")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.dubbing.resource.speaker.findSimilarVoices("dubbing_id", "speaker_id");
        expect(response).toEqual({
            voices: [
                {
                    voiceId: "voice_id",
                    name: "name",
                    category: "premade",
                    description: "description",
                    previewUrl: "preview_url",
                },
            ],
        });
    });

    test("find_similar_voices (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ElevenLabsClient({ apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { detail: undefined };
        server
            .mockEndpoint()
            .get("/v1/dubbing/resource/dubbing_id/speaker/speaker_id/similar-voices")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.dubbing.resource.speaker.findSimilarVoices("dubbing_id", "speaker_id");
        }).rejects.toThrow(ElevenLabs.UnprocessableEntityError);
    });
});
