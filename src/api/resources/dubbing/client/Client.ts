// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import * as environments from "../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import * as serializers from "../../../../serialization/index.js";
import * as ElevenLabs from "../../../index.js";
import { AudioClient } from "../resources/audio/client/Client.js";
import { ProjectClient } from "../resources/project/client/Client.js";
import { TranscriptsClient } from "../resources/transcripts/client/Client.js";

export declare namespace DubbingClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

/**
 * Dub audio and video content into other languages while preserving the original speaker's voice.
 */
export class DubbingClient {
    protected readonly _options: NormalizedClientOptions<DubbingClient.Options>;
    protected _project: ProjectClient | undefined;
    protected _audio: AudioClient | undefined;
    protected _transcripts: TranscriptsClient | undefined;

    constructor(options: DubbingClient.Options = {}) {
        this._options = normalizeClientOptions(options);
    }

    public get project(): ProjectClient {
        return (this._project ??= new ProjectClient(this._options));
    }

    public get audio(): AudioClient {
        return (this._audio ??= new AudioClient(this._options));
    }

    public get transcripts(): TranscriptsClient {
        return (this._transcripts ??= new TranscriptsClient(this._options));
    }

    /**
     * List the dubs you have access to.
     *
     * @param {ElevenLabs.ListDubbingRequest} request
     * @param {DubbingClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link ElevenLabs.UnprocessableEntityError}
     * @throws {@link errors.ElevenLabsError}
     * @throws {@link errors.ElevenLabsTimeoutError}
     *
     * @example
     *     await client.dubbing.list({
     *         cursor: "cursor",
     *         pageSize: 1,
     *         dubbingStatus: "dubbing",
     *         dubbingStatuses: ["queued"],
     *         dubbingModels: ["dubbing_v1"],
     *         targetLanguageCodes: ["target_language_codes"],
     *         creationSources: ["flow_node"],
     *         filterByCreator: "personal",
     *         orderBy: "created_at",
     *         orderDirection: "DESCENDING"
     *     })
     */
    public list(
        request: ElevenLabs.ListDubbingRequest = {},
        requestOptions?: DubbingClient.RequestOptions,
    ): core.HttpResponsePromise<ElevenLabs.DubbingMetadataPageResponseModel> {
        return core.HttpResponsePromise.fromPromise(this.__list(request, requestOptions));
    }

    private async __list(
        request: ElevenLabs.ListDubbingRequest = {},
        requestOptions?: DubbingClient.RequestOptions,
    ): Promise<core.WithRawResponse<ElevenLabs.DubbingMetadataPageResponseModel>> {
        const {
            cursor,
            pageSize,
            dubbingStatus,
            dubbingStatuses,
            dubbingModels,
            targetLanguageCodes,
            creationSources,
            filterByCreator,
            orderBy,
            orderDirection,
        } = request;
        const _queryParams: Record<string, unknown> = {
            cursor,
            page_size: pageSize,
            dubbing_status:
                dubbingStatus != null
                    ? serializers.ListDubbingRequestDubbingStatus.jsonOrThrow(dubbingStatus, {
                          unrecognizedObjectKeys: "strip",
                          omitUndefined: true,
                      })
                    : undefined,
            dubbing_statuses: Array.isArray(dubbingStatuses)
                ? dubbingStatuses.map((item) =>
                      serializers.ListDubbingRequestDubbingStatusesItem.jsonOrThrow(item, {
                          unrecognizedObjectKeys: "strip",
                          omitUndefined: true,
                      }),
                  )
                : dubbingStatuses != null
                  ? serializers.ListDubbingRequestDubbingStatusesItem.jsonOrThrow(dubbingStatuses, {
                        unrecognizedObjectKeys: "strip",
                        omitUndefined: true,
                    })
                  : undefined,
            dubbing_models: Array.isArray(dubbingModels)
                ? dubbingModels.map((item) =>
                      serializers.ListDubbingRequestDubbingModelsItem.jsonOrThrow(item, {
                          unrecognizedObjectKeys: "strip",
                          omitUndefined: true,
                      }),
                  )
                : dubbingModels != null
                  ? serializers.ListDubbingRequestDubbingModelsItem.jsonOrThrow(dubbingModels, {
                        unrecognizedObjectKeys: "strip",
                        omitUndefined: true,
                    })
                  : undefined,
            target_language_codes: targetLanguageCodes,
            creation_sources: Array.isArray(creationSources)
                ? creationSources.map((item) =>
                      serializers.ListDubbingRequestCreationSourcesItem.jsonOrThrow(item, {
                          unrecognizedObjectKeys: "strip",
                          omitUndefined: true,
                      }),
                  )
                : creationSources != null
                  ? serializers.ListDubbingRequestCreationSourcesItem.jsonOrThrow(creationSources, {
                        unrecognizedObjectKeys: "strip",
                        omitUndefined: true,
                    })
                  : undefined,
            filter_by_creator:
                filterByCreator != null
                    ? serializers.ListDubbingRequestFilterByCreator.jsonOrThrow(filterByCreator, {
                          unrecognizedObjectKeys: "strip",
                          omitUndefined: true,
                      })
                    : undefined,
            order_by:
                orderBy != null
                    ? serializers.ListDubbingRequestOrderBy.jsonOrThrow(orderBy, {
                          unrecognizedObjectKeys: "strip",
                          omitUndefined: true,
                      })
                    : undefined,
            order_direction:
                orderDirection != null
                    ? serializers.ListDubbingRequestOrderDirection.jsonOrThrow(orderDirection, {
                          unrecognizedObjectKeys: "strip",
                          omitUndefined: true,
                      })
                    : undefined,
        };
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "xi-api-key": requestOptions?.apiKey ?? this._options?.apiKey ?? process.env?.ELEVENLABS_API_KEY,
            }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.ElevenLabsEnvironment.Production,
                "v1/dubbing",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 240) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.DubbingMetadataPageResponseModel.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new ElevenLabs.UnprocessableEntityError(_response.error.body, _response.rawResponse);
                default:
                    throw new errors.ElevenLabsError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/v1/dubbing");
    }

    /**
     * Dubs a provided audio or video file into given language.
     *
     * @param {ElevenLabs.BodyDubAVideoOrAnAudioFileV1DubbingPost} request
     * @param {DubbingClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link ElevenLabs.UnprocessableEntityError}
     * @throws {@link errors.ElevenLabsError}
     * @throws {@link errors.ElevenLabsTimeoutError}
     *
     * @example
     *     import { createReadStream } from "fs";
     *     await client.dubbing.create({})
     */
    public create(
        request: ElevenLabs.BodyDubAVideoOrAnAudioFileV1DubbingPost,
        requestOptions?: DubbingClient.RequestOptions,
    ): core.HttpResponsePromise<ElevenLabs.DoDubbingResponse> {
        return core.HttpResponsePromise.fromPromise(this.__create(request, requestOptions));
    }

    private async __create(
        request: ElevenLabs.BodyDubAVideoOrAnAudioFileV1DubbingPost,
        requestOptions?: DubbingClient.RequestOptions,
    ): Promise<core.WithRawResponse<ElevenLabs.DoDubbingResponse>> {
        const _body = await core.newFormData();
        if (request.file != null) {
            await _body.appendFile("file", request.file);
        }

        if (request.csvFile != null) {
            await _body.appendFile("csv_file", request.csvFile);
        }

        if (request.foregroundAudioFile != null) {
            await _body.appendFile("foreground_audio_file", request.foregroundAudioFile);
        }

        if (request.backgroundAudioFile != null) {
            await _body.appendFile("background_audio_file", request.backgroundAudioFile);
        }

        if (request.name != null) {
            _body.append("name", request.name);
        }

        if (request.sourceUrl != null) {
            _body.append("source_url", request.sourceUrl);
        }

        if (request.sourceLang != null) {
            _body.append("source_lang", request.sourceLang);
        }

        if (request.targetLang != null) {
            _body.append("target_lang", request.targetLang);
        }

        if (request.targetAccent != null) {
            _body.append("target_accent", request.targetAccent);
        }

        if (request.numSpeakers != null) {
            _body.append("num_speakers", request.numSpeakers?.toString());
        }

        if (request.watermark != null) {
            _body.append("watermark", request.watermark?.toString());
        }

        if (request.startTime != null) {
            _body.append("start_time", request.startTime?.toString());
        }

        if (request.endTime != null) {
            _body.append("end_time", request.endTime?.toString());
        }

        if (request.highestResolution != null) {
            _body.append("highest_resolution", request.highestResolution?.toString());
        }

        if (request.dropBackgroundAudio != null) {
            _body.append("drop_background_audio", request.dropBackgroundAudio?.toString());
        }

        if (request.useProfanityFilter != null) {
            _body.append("use_profanity_filter", request.useProfanityFilter?.toString());
        }

        if (request.dubbingStudio != null) {
            _body.append("dubbing_studio", request.dubbingStudio?.toString());
        }

        if (request.disableVoiceCloning != null) {
            _body.append("disable_voice_cloning", request.disableVoiceCloning?.toString());
        }

        if (request.mode != null) {
            _body.append(
                "mode",
                serializers.DubRequestMode.jsonOrThrow(request.mode, {
                    unrecognizedObjectKeys: "strip",
                    omitUndefined: true,
                }),
            );
        }

        if (request.csvFps != null) {
            _body.append("csv_fps", request.csvFps?.toString());
        }

        const _maybeEncodedRequest = await _body.getRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "xi-api-key": requestOptions?.apiKey ?? this._options?.apiKey ?? process.env?.ELEVENLABS_API_KEY,
                ..._maybeEncodedRequest.headers,
            }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.ElevenLabsEnvironment.Production,
                "v1/dubbing",
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "file",
            duplex: _maybeEncodedRequest.duplex,
            body: _maybeEncodedRequest.body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 240) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.DoDubbingResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new ElevenLabs.UnprocessableEntityError(_response.error.body, _response.rawResponse);
                default:
                    throw new errors.ElevenLabsError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/v1/dubbing");
    }

    /**
     * Returns metadata about a dubbing project, including whether it's still in progress or not
     *
     * @param {string} dubbing_id - ID of the dubbing project.
     * @param {DubbingClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link ElevenLabs.UnprocessableEntityError}
     * @throws {@link errors.ElevenLabsError}
     * @throws {@link errors.ElevenLabsTimeoutError}
     *
     * @example
     *     await client.dubbing.get("dubbing_id")
     */
    public get(
        dubbing_id: string,
        requestOptions?: DubbingClient.RequestOptions,
    ): core.HttpResponsePromise<ElevenLabs.DubbingMetadataResponse> {
        return core.HttpResponsePromise.fromPromise(this.__get(dubbing_id, requestOptions));
    }

    private async __get(
        dubbing_id: string,
        requestOptions?: DubbingClient.RequestOptions,
    ): Promise<core.WithRawResponse<ElevenLabs.DubbingMetadataResponse>> {
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "xi-api-key": requestOptions?.apiKey ?? this._options?.apiKey ?? process.env?.ELEVENLABS_API_KEY,
            }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.ElevenLabsEnvironment.Production,
                `v1/dubbing/${core.url.encodePathParam(dubbing_id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 240) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.DubbingMetadataResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new ElevenLabs.UnprocessableEntityError(_response.error.body, _response.rawResponse);
                default:
                    throw new errors.ElevenLabsError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/v1/dubbing/{dubbing_id}");
    }

    /**
     * Deletes a dubbing project.
     *
     * @param {string} dubbing_id - ID of the dubbing project.
     * @param {DubbingClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link ElevenLabs.UnprocessableEntityError}
     * @throws {@link errors.ElevenLabsError}
     * @throws {@link errors.ElevenLabsTimeoutError}
     *
     * @example
     *     await client.dubbing.delete("dubbing_id")
     */
    public delete(
        dubbing_id: string,
        requestOptions?: DubbingClient.RequestOptions,
    ): core.HttpResponsePromise<ElevenLabs.DeleteDubbingResponseModel> {
        return core.HttpResponsePromise.fromPromise(this.__delete(dubbing_id, requestOptions));
    }

    private async __delete(
        dubbing_id: string,
        requestOptions?: DubbingClient.RequestOptions,
    ): Promise<core.WithRawResponse<ElevenLabs.DeleteDubbingResponseModel>> {
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "xi-api-key": requestOptions?.apiKey ?? this._options?.apiKey ?? process.env?.ELEVENLABS_API_KEY,
            }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.ElevenLabsEnvironment.Production,
                `v1/dubbing/${core.url.encodePathParam(dubbing_id)}`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 240) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.DeleteDubbingResponseModel.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new ElevenLabs.UnprocessableEntityError(_response.error.body, _response.rawResponse);
                default:
                    throw new errors.ElevenLabsError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "DELETE", "/v1/dubbing/{dubbing_id}");
    }
}
