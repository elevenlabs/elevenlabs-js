// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import { toJson } from "../../../../core/json.js";
import * as environments from "../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import * as serializers from "../../../../serialization/index.js";
import * as ElevenLabs from "../../../index.js";
import { TranscriptsClient } from "../resources/transcripts/client/Client.js";
import { SpeechToTextSocket } from "./Socket.js";

export declare namespace SpeechToTextClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}

    export interface ConnectArgs {
        modelId: string;
        token?: string;
        audioFormat?: string;
        languageCode?: string;
        secondaryLanguages?: string;
        commitStrategy?: string;
        vadThreshold?: string;
        vadSilenceThresholdSecs?: string;
        minSpeechDurationMs?: string;
        minSilenceDurationMs?: string;
        includeTimestamps?: string;
        includeLanguageDetection?: string;
        keyterms?: string;
        noVerbatim?: string;
        entityDetection?: string;
        filterBackgroundAudio?: string;
        enableLogging?: string;
        /** WebSocket subprotocols to use for the connection. */
        protocols?: string | string[];
        /** Additional query parameters to send with the websocket connect request. */
        queryParams?: Record<string, unknown>;
        /** Arbitrary headers to send with the websocket connect request. */
        headers?: Record<string, string>;
        /** Enable debug mode on the websocket. Defaults to false. */
        debug?: boolean;
        /** Number of reconnect attempts. Defaults to 30. */
        reconnectAttempts?: number;
        /** The timeout for establishing the WebSocket connection in seconds. */
        connectionTimeoutInSeconds?: number;
        /** A signal to abort the WebSocket connection. */
        abortSignal?: AbortSignal;
    }
}

export class SpeechToTextClient {
    protected readonly _options: NormalizedClientOptions<SpeechToTextClient.Options>;
    protected _transcripts: TranscriptsClient | undefined;

    constructor(options: SpeechToTextClient.Options = {}) {
        this._options = normalizeClientOptions(options);
    }

    public get transcripts(): TranscriptsClient {
        return (this._transcripts ??= new TranscriptsClient(this._options));
    }

    /**
     * Transcribe an audio or video file. If webhook is set to true, the request will be processed asynchronously and results sent to configured webhooks. When use_multi_channel is true and the provided audio has multiple channels, a 'transcripts' object with separate transcripts for each channel is returned; set multichannel_output_style='combined' to instead receive a single transcript with all channels merged and sorted by time. Otherwise, returns a single transcript. The optional webhook_metadata parameter allows you to attach custom data that will be included in webhook responses for request correlation and tracking.
     *
     * @param {ElevenLabs.BodySpeechToTextV1SpeechToTextPost} request
     * @param {SpeechToTextClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link ElevenLabs.UnprocessableEntityError}
     * @throws {@link errors.ElevenLabsError}
     * @throws {@link errors.ElevenLabsTimeoutError}
     *
     * @example
     *     import { createReadStream } from "fs";
     *     await client.speechToText.convert({
     *         token: "token",
     *         enableLogging: true,
     *         modelId: "scribe_v2"
     *     })
     */
    public convert(
        request: ElevenLabs.BodySpeechToTextV1SpeechToTextPost,
        requestOptions?: SpeechToTextClient.RequestOptions,
    ): core.HttpResponsePromise<ElevenLabs.ConvertSpeechToTextResponse> {
        return core.HttpResponsePromise.fromPromise(this.__convert(request, requestOptions));
    }

    private async __convert(
        request: ElevenLabs.BodySpeechToTextV1SpeechToTextPost,
        requestOptions?: SpeechToTextClient.RequestOptions,
    ): Promise<core.WithRawResponse<ElevenLabs.ConvertSpeechToTextResponse>> {
        const _queryParams: Record<string, unknown> = {
            token: request.token,
            enable_logging: request.enableLogging,
        };
        const _body = await core.newFormData();
        _body.append("model_id", request.modelId);
        if (request.file != null) {
            await _body.appendFile("file", request.file);
        }

        if (request.languageCode != null) {
            _body.append("language_code", request.languageCode);
        }

        if (request.tagAudioEvents != null) {
            _body.append("tag_audio_events", request.tagAudioEvents?.toString());
        }

        if (request.numSpeakers != null) {
            _body.append("num_speakers", request.numSpeakers?.toString());
        }

        if (request.timestampsGranularity != null) {
            _body.append(
                "timestamps_granularity",
                serializers.ConvertSpeechToTextRequestTimestampsGranularity.jsonOrThrow(request.timestampsGranularity, {
                    unrecognizedObjectKeys: "strip",
                    omitUndefined: true,
                }),
            );
        }

        if (request.diarize != null) {
            _body.append("diarize", request.diarize?.toString());
        }

        if (request.diarizationThreshold != null) {
            _body.append("diarization_threshold", request.diarizationThreshold?.toString());
        }

        if (request.additionalFormats != null) {
            _body.append("additional_formats", toJson(request.additionalFormats));
        }

        if (request.fileFormat != null) {
            _body.append(
                "file_format",
                serializers.ConvertSpeechToTextRequestFileFormat.jsonOrThrow(request.fileFormat, {
                    unrecognizedObjectKeys: "strip",
                    omitUndefined: true,
                }),
            );
        }

        if (request.sourceUrl != null) {
            _body.append("source_url", request.sourceUrl);
        }

        if (request.webhook != null) {
            _body.append("webhook", request.webhook?.toString());
        }

        if (request.webhookId != null) {
            _body.append("webhook_id", request.webhookId);
        }

        if (request.temperature != null) {
            _body.append("temperature", request.temperature?.toString());
        }

        if (request.seed != null) {
            _body.append("seed", request.seed?.toString());
        }

        if (request.useMultiChannel != null) {
            _body.append("use_multi_channel", request.useMultiChannel?.toString());
        }

        if (request.multichannelOutputStyle != null) {
            _body.append(
                "multichannel_output_style",
                serializers.ConvertSpeechToTextRequestMultichannelOutputStyle.jsonOrThrow(
                    request.multichannelOutputStyle,
                    { unrecognizedObjectKeys: "strip", omitUndefined: true },
                ),
            );
        }

        if (request.webhookMetadata != null) {
            _body.append(
                "webhook_metadata",
                (() => {
                    const mapped = serializers.ConvertSpeechToTextRequestWebhookMetadata.jsonOrThrow(
                        request.webhookMetadata,
                        { unrecognizedObjectKeys: "strip", omitUndefined: true },
                    );
                    return typeof mapped === "string" ? mapped : toJson(mapped);
                })(),
            );
        }

        if (request.entityDetection != null) {
            if (Array.isArray(request.entityDetection))
                for (const _item of request.entityDetection) {
                    _body.append("entity_detection", typeof _item === "string" ? _item : toJson(_item));
                }
        }

        if (request.noVerbatim != null) {
            _body.append("no_verbatim", request.noVerbatim?.toString());
        }

        if (request.useSpeakerLibrary != null) {
            _body.append("use_speaker_library", request.useSpeakerLibrary?.toString());
        }

        if (request.detectSpeakerRoles != null) {
            _body.append("detect_speaker_roles", request.detectSpeakerRoles?.toString());
        }

        if (request.entityRedaction != null) {
            if (Array.isArray(request.entityRedaction))
                for (const _item of request.entityRedaction) {
                    _body.append("entity_redaction", typeof _item === "string" ? _item : toJson(_item));
                }
        }

        if (request.entityRedactionMode != null) {
            _body.append("entity_redaction_mode", request.entityRedactionMode);
        }

        if (request.keyterms != null) {
            for (const _item of request.keyterms) {
                _body.append("keyterms", _item);
            }
        }

        const _maybeEncodedRequest = await _body.getRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "xi-api-key": requestOptions?.apiKey ?? this._options?.apiKey ?? process.env?.ELEVENLABS_API_KEY,
                ..._maybeEncodedRequest.headers,
            }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.ElevenLabsEnvironment.Production,
                "v1/speech-to-text",
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            requestType: "file",
            duplex: _maybeEncodedRequest.duplex,
            body: _maybeEncodedRequest.body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 240) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.ConvertSpeechToTextResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new ElevenLabs.UnprocessableEntityError(_response.error.body, _response.rawResponse);
                default:
                    throw new errors.ElevenLabsError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/v1/speech-to-text");
    }

    public async realtime(args: SpeechToTextClient.ConnectArgs): Promise<SpeechToTextSocket> {
        const {
            modelId,
            token,
            audioFormat,
            languageCode,
            secondaryLanguages,
            commitStrategy,
            vadThreshold,
            vadSilenceThresholdSecs,
            minSpeechDurationMs,
            minSilenceDurationMs,
            includeTimestamps,
            includeLanguageDetection,
            keyterms,
            noVerbatim,
            entityDetection,
            filterBackgroundAudio,
            enableLogging,
            protocols,
            queryParams,
            headers,
            debug,
            reconnectAttempts,
            connectionTimeoutInSeconds,
            abortSignal,
        } = args;
        const _queryParams: Record<string, unknown> = {
            model_id: modelId,
            token,
            audio_format: audioFormat,
            language_code: languageCode,
            secondary_languages: secondaryLanguages,
            commit_strategy: commitStrategy,
            vad_threshold: vadThreshold,
            vad_silence_threshold_secs: vadSilenceThresholdSecs,
            min_speech_duration_ms: minSpeechDurationMs,
            min_silence_duration_ms: minSilenceDurationMs,
            include_timestamps: includeTimestamps,
            include_language_detection: includeLanguageDetection,
            keyterms,
            no_verbatim: noVerbatim,
            entity_detection: entityDetection,
            filter_background_audio: filterBackgroundAudio,
            enable_logging: enableLogging,
        };
        const _headers: Record<string, unknown> = mergeHeaders(this._options?.headers, headers);
        const socket = new core.ReconnectingWebSocket({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.ElevenLabsEnvironment.Production,
                "/v1/speech-to-text/realtime",
            ),
            protocols: protocols ?? [],
            queryParameters: { ..._queryParams, ...queryParams },
            headers: _headers,
            options: {
                debug: debug ?? false,
                maxRetries: reconnectAttempts ?? 30,
                connectionTimeout: connectionTimeoutInSeconds != null ? connectionTimeoutInSeconds * 1000 : undefined,
            },
            abortSignal: abortSignal,
        });
        return new SpeechToTextSocket({ socket });
    }
}
