// This file was auto-generated by Fern from our API Definition.

import type * as ElevenLabs from "../index.js";

/**
 * Client -> server message for the multi-context endpoint.
 *
 * Adds a ``context_id`` so several independent dialogue streams can be
 * multiplexed over a single socket. ``voices``/``voice_settings``/
 * ``pronunciation_dictionary_locators`` apply to a context's first message
 * (not the connection).
 */
export interface TextToDialogueMultiClientMessage {
    inputs?: ElevenLabs.TextToDialogueMultiClientMessageInputsItem[];
    flush?: boolean;
    closeSocket?: boolean;
    keepAlive?: boolean;
    xiApiKey?: string;
    authorization?: string;
    singleUseToken?: string;
    /** List of voice IDs to register for the connection. */
    voices?: string[];
    /** Voice settings for the connection. */
    voiceSettings?: ElevenLabs.TextToDialogueMultiClientMessageVoiceSettings;
    pronunciationDictionaryLocators?: ElevenLabs.TextToDialogueMultiClientMessagePronunciationDictionaryLocatorsItem[];
    /** Context ID for an independent dialogue stream within the socket. */
    contextId?: string;
    /** Flush and close this context, leaving other contexts open. */
    closeContext?: boolean;
}
